#include <stdio.h>

int main() {
    int numero;

    printf("Digite um numero: ");
    scanf("%d", &numero);

    // Verifica se é múltiplo de 3
    if (numero % 3 == 0) {
        printf("O numero %d é multiplo de 3\n", numero);
    }

    return 0;
}
